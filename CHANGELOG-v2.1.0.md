# v2.1.0 更新日志

发布日期：2026-07-09

## 首页与导航

- 重构首页工作台，优化设备概览、当前应用、常用操作和系统指标的信息层级。
- 常用操作图标改为多色区分，CPU、存储、内存、电量等指标卡片增强识别度。
- 命令页面恢复为左侧 Tab 入口，并移除首页中的“更多”入口。
- 当前应用卡片支持点击跳转到对应应用详情。

## 设备控制

- 新增设备控制聚合页，将按键模拟和设备镜像合并展示，减少 Tab 切换成本。
- 移除设备控制顶部标题卡片，让页面更贴近工具型布局。
- 镜像状态与设置入口合并到同一区块，设置项通过独立弹窗管理。
- 镜像设置默认关闭“始终置顶”。
- 镜像高级参数下拉图标调整为更小的线性图标，改善视觉协调性。
- 镜像设置弹窗改为固定尺寸自定义 Dialog，避免自定义模式滚动时拉伸页面。

## 录屏

- 新增录屏设置弹窗，支持手动停止和固定时长两种录制方式。
- 手动模式下录屏不再固定时长，由用户主动停止。
- 录屏中新增状态弹窗，展示录制时长或倒计时，并提供停止入口。
- 优化录屏弹窗按钮配色、选项布局和焦点反馈，统一首页风格。

## 日志与进程

- 日志页面标题降级，整体信息密度更适合诊断工具场景。
- 日志筛选区、操作区和列表容器收紧间距与圆角。
- 进程页面改为紧凑标题、搜索区、统计胶囊和表格容器的结构。
- 进程刷新按钮改为小号图标按钮，降低视觉重量。

## 桌面构建与平台体验

- 桌面发行流程补充 Linux `.deb`、`.rpm`、`.tar.gz` 产物。
- Windows 安装包和便携包文件名追加应用版本号。
- Windows 便携包构建流程补齐，方便免安装使用。
- 增加 `-Dawt.useSystemAAFontSettings=on` JVM 参数，改善部分 Windows 环境字体抗锯齿和清晰度。
- Linux 镜像功能支持通过系统 `scrcpy` 或 `SCRCPY_PATH` 环境变量回退查找。

## 应用与图标

- 应用列表和应用详情加载链路继续优化，补充前台应用和应用详情跳转体验。
- 新增 icon helper 资源同步，提升 Android 应用图标读取和展示能力。
